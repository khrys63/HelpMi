spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${APP_KEYCLOAK_ISSUER_URI}

# APP_CORS_ALLOWED_ORIGINS must be set to the frontend URL (e.g. https://helpmi.example.com)
app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS}
